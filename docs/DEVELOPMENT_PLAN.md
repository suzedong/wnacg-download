# WNACG Downloader - 开发计划

## 项目状态

**当前版本**: v4.0（纯桌面端）
**技术栈**: Rust + Tauri 2 + Vue 3 + TypeScript
**整体完成度**: 约 85-90%

---

## 现状概览

### 已完成的核心功能

#### 后端（Rust）- 100%
- ✅ 配置管理系统（读写 JSON、默认配置、系统路径）
- ✅ 事件系统（5 种事件类型、进度推送）
- ✅ 错误处理（完整的错误类型覆盖）
- ✅ Rust 后端核心模块（downloader, scanner, comparer, ai）
- ✅ 搜索模块（Node.js + Playwright 脚本）
- ✅ scraper 模块（保留代码，当前未使用）
- ✅ Tauri Commands（search、download、compare、config、open_folder 全部实现）
- ✅ AI 匹配两阶段流程：本地精确/模糊匹配优先 + AI 仅兜底未匹配漫画（解决 81 部漫画一次性 AI 调用慢的问题）
- ✅ AI API 未配置时，未匹配漫画自动标记为 `need_download`
- ✅ 下载源优先策略配置（Server 2 / Worker API）
- ✅ 双策略下载：Server 2 直连（reqwest） / Worker API 单浏览器一步完成（绕过 Cloudflare）
- ✅ 系统托盘（显示窗口、退出应用、点击图标恢复窗口）
- ✅ 窗口控制（最小化、最大化、关闭隐藏）

#### 前端（Vue 3）- 80%
- ✅ 应用框架（Vue 3 + TypeScript + Vite）
- ✅ 主布局（App.vue、侧边栏导航）
- ✅ 主题系统（亮色/暗色模式、CSS 变量）
- ✅ 侧边栏（4 个导航项、主题切换按钮）
- ✅ Composables（useSearch、useDownload、useCompare、useConfig、useDownloadQueue）
- ✅ 类型系统（types/index.ts 统一定义、前后端一致）
- ✅ 配置页面（完整表单、所有配置项、文件夹选择对话框）
- ✅ 搜索页面（搜索结果展示、历史记录、队列联动、预览跳转）
- ✅ 对比页面（统计卡片、对比结果展示、需下载/已拥有分类）
- ✅ 下载页面（下载队列、进度显示、控制按钮）

---

## 待完成的任务（按优先级排序）

### Phase 1：完善现有功能（P0 - 高优先级）

**目标**：完善已实现页面的细节功能和用户体验

#### 任务清单

- [x] **1.1 搜索页面细节完善**
  - [x] 重新搜索按钮（获取最新数据）
  - [x] 清空搜索结果按钮
  - [x] 当前搜索信息显示（关键字 + 时间）

- [ ] **1.2 对比页面细节完善**
  - [x] 将对比结果中的"需下载"漫画添加到下载队列
  - [ ] 优化匹配结果的展示和排序

- [x] **1.3 下载页面细节完善**
  - [x] 暂停/恢复下载（后端 pause_download/resume_download + 前端 UI）
  - [x] 取消单个下载任务（后端 cancel_task + 前端 UI）
  - [x] 重试失败任务（UI 已实现，aid 使用标题作为占位符）
  - [x] 下载完成通知（Rust 后端原生通知 + 前端 Toast 提示）

- [x] **1.4 全局下载队列管理**
  - [x] 队列去重（Set 过滤重复 aid）
  - [x] 队列持久化（可选，localStorage 按需实现）

#### 验收标准
- [x] 搜索页面可以重新搜索和清空结果
- [x] 对比页面可以将需下载漫画添加到下载队列
- [x] 下载页面支持暂停、取消、重试操作
- [x] 三个页面之间数据流转正常

---

### Phase 2：完善用户体验（P1 - 中优先级）

**目标**：优化界面细节、提升交互体验

#### 任务清单

- [x] **2.1 暗色模式跟随系统**
  - [x] 检测 `prefers-color-scheme` 媒体查询
  - [x] 添加"跟随系统"选项到配置页面
  - [x] 监听系统主题变化事件（`window.matchMedia`）
  - [x] 实现主题自动切换
  - [x] 更新配置保存逻辑

- [x] **2.2 文件/文件夹选择对话框**
  - [x] 对比页面：选择搜索缓存文件（`<select>` 下拉框）
  - [x] 对比页面：选择本地文件夹
  - [x] 配置页面：选择默认存储路径
  - [x] 使用 `@tauri-apps/plugin-dialog` 的 `open` API

- [x] **2.3 错误提示优化**
  - [x] 统一错误提示组件（Toast/Notification）
  - [x] 网络错误提供重试按钮（搜索/对比/下载页面 Toast 带重试）
  - [x] 下载失败显示具体原因（Toast 显示后端错误详情）
  - [x] 配置错误提供修复建议（存储路径/代理/AI 地址验证）
  - [x] 使用 Tauri 通知 API 发送系统通知（Rust 后端已实现原生通知）

- [x] **2.4 加载状态优化**
  - [x] 添加骨架屏（Skeleton）组件（搜索/对比页面）
  - [x] 搜索加载动画（进度条 + 进度文字）
  - [x] 对比加载动画（AI 等待 Spinner + 流式内容）
  - [x] 下载队列加载状态（进度条 + 速度显示）
  - [x] 超时处理和取消按钮（Toast 带重试按钮，支持取消当前操作）

- [x] **2.5 快捷键支持**
  - [x] `Ctrl+1/2/3/4` 切换页面
  - [x] `Ctrl+D` 切换暗色模式
  - [x] `Ctrl+S` 聚焦搜索框
  - [x] `Escape` 取消当前操作

- [x] **2.6 配置页面优化**
  - [x] 补全缺失配置项（max_pages、search_chinese_only）
  - [x] 重新组织为 6 个分区（存储/搜索/网络/下载/AI/外观）
  - [x] 数值范围验证和实时反馈
  - [x] 自动保存（800ms 防抖，修改即保存）
  - [x] 自定义 Toggle Switch（紫色渐变主题）
  - [x] 分段按钮组（主题切换）
  - [x] 保存状态指示器（已同步/正在保存/未保存）

#### 验收标准
- [x] 暗色模式可跟随系统自动切换
- [x] 文件/文件夹选择对话框正常工作
- [x] 错误提示友好且可操作
- [x] 加载状态清晰明确
- [x] 配置页面补全缺失项并实现自动保存

---

### Phase 3：测试与优化（P2 - 低优先级）

**目标**：编写测试、性能优化、修复边界情况

#### 任务清单

- [x] **3.1 Rust 核心模块单元测试**
  - [ ] `scraper` 模块：HTML 解析、去重逻辑、汉化过滤（已标记 dead_code，暂不测试）
  - [x] `downloader` 模块：HTML 实体解码、Server 2 URL 拼接
  - [x] `comparer` 模块：相似度计算、匹配算法（通过 ai 模块覆盖）
  - [x] `ai` 模块：Prompt 构建、响应解析
  - [x] `scanner` 模块：目录扫描、文件识别

- [x] **3.2 前端组件测试**
  - [x] Composables 测试（useDownloadQueue）
  - [x] 工具函数测试（formatBytes、formatSpeed、formatETA）
  - [ ] 组件渲染测试（需 jsdom 深入配置）
  - [ ] 用户交互测试（需 Tauri mock）

- [ ] **3.3 端到端测试** — 跳过（需要 Tauri 完整运行时）
- [ ] **3.4 性能优化** — 跳过（待实际使用反馈后再针对性优化）
- [ ] **3.5 边界情况处理** — 跳过（下载队列去重已实现，其余待实际使用反馈）

#### 验收标准
- [x] 核心模块测试覆盖率 > 70%（纯函数 100% 覆盖）
- [x] 所有测试用例通过（69 个）
- [ ] 性能指标达标
- [ ] 边界情况有妥善处理

---

### Phase 4：打包与发布（P3 - 最后阶段）

**目标**：完善打包配置、发布准备

#### 任务清单

- [x] **4.1 系统托盘实现**
  - [x] 创建托盘图标
  - [x] 托盘菜单：显示/隐藏、退出
  - [x] 最小化到托盘

- [x] **4.2 应用图标完善**
  - [x] 应用图标已就位（ico/icns/png/svg 全覆盖）

- [x] **4.3 打包配置**
  - [x] 配置 NSIS 安装包（Windows，中英双语安装向导）
  - [x] 配置 DMG 打包（macOS，图标位置已优化）
  - [x] 添加版本信息和版权信息（Cargo.toml 元数据完善）
  - [ ] 配置自动更新（可选）

- [x] **4.4 文档完善**
  - [x] 更新 README.md 添加使用说明、配置项速查、快捷键、FAQ
  - [x] 新建用户使用手册（docs/USER_MANUAL.md）
  - [x] 新建贡献指南（docs/CONTRIBUTING.md）
  - [x] 更新文档中心（docs/README.md）

- [ ] **4.5 发布准备**
  - [ ] 创建发布版本标签
  - [ ] 编写 Release Notes
  - [ ] 准备安装包
  - [ ] 测试安装包在不同系统的兼容性

#### 验收标准
- [x] 系统托盘正常工作
- [ ] Windows 安装包可正常安装和运行
- [ ] macOS DMG 可正常安装和运行
- [ ] README 和文档完整
- [ ] 版本号正确

---

## 优先级总结

| 优先级 | Phase | 核心任务 | 预计影响 | 当前状态 |
|--------|-------|----------|----------|----------|
| **P0** | Phase 1 | 完善现有页面功能 | 功能闭环 | ✅ 全部完成 |
| **P1** | Phase 2 | 完善用户体验 | 用户友好 | ✅ 全部完成 |
| **P2** | Phase 3 | 测试优化 | 稳定可靠 | ✅ 已完成 |
| **P3** | Phase 4 | 打包发布 | 正式发布 | 🟡 文档完成，打包进行中 |

---

## 关键问题与风险

### 已知问题
1. **Worker API 下载需要非 headless 浏览器**：Cloudflare 检测 headless Chromium 并拦截，浏览器窗口会短暂可见

### 技术风险
1. **WNACG 网站结构变化**：HTML 解析可能失效
2. **AI API 成本控制**：已优化为「本地优先 + AI 兜底」，大部分情况零 API 调用
3. **并发下载稳定性**：大量任务时可能遇到资源竞争
4. **跨平台兼容性**：Windows 和 macOS 的托盘、通知差异

---

## 开发建议

1. **严格按照 Phase 顺序执行**，不要跳过
2. **Phase 1 是核心**，完成页面细节后功能基本闭环
3. **每个任务完成后立即验证**，不要堆积
4. **遇到问题先查文档**，不要盲目尝试
5. **保持代码整洁**，及时提交
6. **中文注释和日志**，保持一致性

---

**最后更新**: 2026-05-19（文档完善：README 重写、用户手册、贡献指南）
**版本**: v4.1（下载源策略优化版）
